/**
 * Game Hub Layout
 * Layout cho game hub section
 */

export default function GameHubLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="game-hub-layout">
      {children}
    </div>
  );
}
